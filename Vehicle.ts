// Vehicle.ts
export interface Vehicle {
    make: string;
    model: string;
    year: number;
    start(): void;
  }
  
//Vehicle Interface: The Vehicle interface defines the structure for a vehicle object with make, model, year properties, and a start method.